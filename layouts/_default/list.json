[
  {{- $len := (len .Pages) -}}
  {{- range $i, $page := .Pages -}}
  {
    "title": "{{ .Title }}",
    "permalink": "{{ .Permalink }}",
    "summary": "{{ .Summary | plainify }}",
    "content": "{{ .Plain | plainify }}"
  }{{- if ne (add $i 1) $len -}},{{- end -}}
  {{- end -}}
]